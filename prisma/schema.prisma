generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // hoặc mysql/sqlite (tuỳ bạn)

model Issue {
  id           String  @id @default(uuid())
  status       String
  priority     String
  due_date     String?
  assigned_to  String?
}